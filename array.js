var studentAge = [15, 18, 20, 25];

var wazidsAge = studentAge[2];        // find value of index

var position = studentAge.indexOf(20);  // find index number


console.log(studentAge.length);            // to see total element in an array

studentAge.push(26);             // to insert an element
console.log(studentAge);

studentAge.unshift(15, 33);        // to add element at the beginning

studentAge.pop();                   // to delete
console.log(studentAge);

studentAge.shift();                 // detete first element at the beginning
console.log(studentAge);






